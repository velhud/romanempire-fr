romanempire.fr — one-page site

Files:
- index.html — main page
- /assets/hero.jpg — put your full-bleed portrait here (optional). A vector fallback hero.svg is provided.
- /assets/hero.svg — gradient fallback background
- /assets/logos/*.svg — replace with your logos (SVG or PNG)
- robots.txt, sitemap.xml — basic SEO hygiene

Edit points:
- index.html: search for 'Implementation notes' at bottom.
- CSS tokens live in :root.
- Email address is 'ave@romanempire.fr' in two places (header Contact, hero contact).

Deploy on any static host. For GitHub Pages, place files at repo root and enable Pages. For other hosts, just upload everything.
